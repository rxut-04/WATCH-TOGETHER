# Netlify: TanStack Start with @netlify/vite-plugin-tanstack-start
[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# SPA mode: serve shell for all routes (no hydration)
[[redirects]]
  from = "/*"
  to = "/_shell.html"
  status = 200
