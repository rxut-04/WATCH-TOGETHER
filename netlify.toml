# Netlify: npm run build, publish dist/client
# For full SSR: use @netlify/vite-plugin-tanstack-start per TanStack docs
[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
